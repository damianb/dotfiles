def playerIcons(poi):
    if poi['id'] == 'Player':
        poi['icon'] = "http://overviewer.org/avatar/%s" % poi['EntityId']
        return "Last known location for %s" % poi['EntityId']

def signFilter(poi):
    if poi['id'] == 'Sign':
        return "\n".join([poi['Text1'], poi['Text2'], poi['Text3'], poi['Text4']])

worlds["sierra"] = "/home/katana/.minecraft/saves/sierra/"

renders["normalrender"] = {
    "world": "sierra",
    "title": "sierra overworld render",
    "rendermode": smooth_lighting,
    "dimension": "overworld",
    'markers': [dict(name="All signs", filterFunction=signFilter),
         dict(name="Players", filterFunction=playerIcons)],
}
renders["nightrender"] = {
    "world": "sierra",
    "title": "sierra overworld night render",
    "rendermode": smooth_night,
    "dimension": "overworld",
   'markers': [dict(name="All signs", filterFunction=signFilter),
         dict(name="Players", filterFunction=playerIcons)],
}
renders["caverender"] = {
	"world": "sierra",
	"title": "sierra cave render",
	"rendermode": cave,
	"dimension": "overworld",
}

outputdir = "/home/katana/minecraftmap/"
